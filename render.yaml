services:
  - type: web
    name: gaffers-guide
    env: python
    region: oregon
    plan: free

    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn cleaned_app:app

    build:
      pythonVersion: 3.10

    autoDeploy: true