# Football computer vision – core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
SoccerNet>=0.1.60
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
supervision>=0.19.0
scikit-learn>=1.3.0
opencv-python>=4.8.0
numpy>=1.24.0
notebook>=7.0.0

# Dynamic pitch calibration (SoccerNet/sn-calibration reference)
# Existing torch, torchvision, opencv-python, numpy cover calibrator usage.
# Optional for running reference scripts (detect_extremities, baseline_cameras):
pillow>=9.0.0
tqdm>=4.62.0
